# Documentation instructions

In every shell example, put the command and positional arguments on the first
line. Put each flag with its value on its own continuation line. End each
continued line with a backslash.
