interface:
  display_name: "Seqlane ADR Delivery"
  short_description: "Deliver ADR tasks through verified commits"
  default_prompt: "Use $seqlane-adr-delivery to implement a named ADR through its linked spec and tasks."
