{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "read-context",
  "projectType": "library",
  "sourceRoot": "libs/read-context/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/read-context",
        "command": "tsc --build tsconfig.json"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/read-context",
        "command": "vitest run"
      }
    }
  }
}
