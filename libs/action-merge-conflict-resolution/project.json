{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "action-merge-conflict-resolution",
  "projectType": "library",
  "sourceRoot": "libs/action-merge-conflict-resolution/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "cwd": "libs/action-merge-conflict-resolution",
        "command": "tsc --build tsconfig.json"
      },
      "cache": true,
      "inputs": ["bundleProduction", "^bundleProduction", "bundleGlobals"],
      "outputs": ["{projectRoot}/dist"]
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "cwd": "libs/action-merge-conflict-resolution",
        "command": "vitest run"
      }
    }
  }
}
