{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "seqlane-studio-service",
  "projectType": "library",
  "sourceRoot": "libs/seqlane-studio/src",
  "tags": ["type:lib", "scope:private"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/seqlane-studio",
        "command": "tsc --build tsconfig.json"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/seqlane-studio",
        "command": "vitest run"
      }
    }
  }
}
