{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "seqlane-events",
  "projectType": "library",
  "sourceRoot": "libs/seqlane-events/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/seqlane-events",
        "command": "tsc --build tsconfig.json"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/seqlane-events",
        "command": "vitest run"
      }
    }
  }
}
