# Local-only example

Shows a deterministic task with no model or workspace access.

Run: `seqlane run workflows/local-only-example/workflow.ts --input '{"value":"hello"}'`

Input and output are `{ value: string }`. It requires only the Seqlane CLI.
This is a visibly marked authoring example, not a consumer adapter or published
package.
