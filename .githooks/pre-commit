#!/bin/sh
set -eu

if ! command -v pnpm >/dev/null 2>&1; then
  echo "pnpm is required for Seqlane pre-commit checks." >&2
  exit 1
fi

root=$(git rev-parse --show-toplevel)
cd "$root"
exec pnpm verify:staged
