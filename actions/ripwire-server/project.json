{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "action-ripwire-server",
  "projectType": "application",
  "sourceRoot": "actions/ripwire-server/src",
  "implicitDependencies": ["action-service-lifecycle"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "actions/ripwire-server",
        "command": "tsc --noEmit --project tsconfig.json"
      },
      "dependsOn": ["^build"]
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "actions/ripwire-server",
        "command": "pnpm exec esbuild src/main.ts --bundle --platform=node --target=node24 --format=esm --banner:js='import { createRequire } from \"node:module\"; globalThis.require ??= createRequire(import.meta.url);' --outfile=dist/main.js --minify && pnpm exec esbuild src/post.ts --bundle --platform=node --target=node24 --format=esm --banner:js='import { createRequire } from \"node:module\"; globalThis.require ??= createRequire(import.meta.url);' --outfile=dist/post.js --minify && node scripts/normalize-bundles.mjs dist/main.js dist/post.js"
      },
      "cache": true,
      "inputs": ["bundleProduction", "^bundleProduction", "bundleGlobals"],
      "outputs": ["{projectRoot}/dist/main.js", "{projectRoot}/dist/post.js"],
      "dependsOn": ["typecheck", "^build"]
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "cwd": "actions/ripwire-server",
        "command": "node --check process-anchor.js && pnpm exec vitest run src/config.spec.ts src/commands.spec.ts src/release.spec.ts src/readiness.spec.ts src/lifecycle.spec.ts src/port.spec.ts src/socket-ownership.spec.ts src/post.spec.ts src/state.spec.ts src/token.spec.ts src/main.spec.ts src/bundle-runtime.spec.ts"
      }
    }
  }
}
